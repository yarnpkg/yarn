// Check if a module has any dependencies in it's `package.json` that aren't actually
// referenced in code. This could be due to either oversight, binary0only usage or
// inserted by a rogue npm worm.
